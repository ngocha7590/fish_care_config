server:
 port: 9601
eureka:
 instance:
  hostname: localhost
 client:
  registeWithEureka: false
  fetchRegistry: false
  serviceUrl:
   defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka