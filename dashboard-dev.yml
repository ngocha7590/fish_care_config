host:
  eureka: localhost
  keycloak: 45.76.96.92
  mysql: localhost

server:
  port: 9602

spring:
  datasource:
    url: 'jdbc:mysql://${host.mysql}:3306/fishcare?useSSL=false&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC&allowPublicKeyRetrieval=true'
    username: root
    password: 123456789
  jpa:
    hibernate:
      ddl-auto: update
  main:
    allow-bean-definition-overriding: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://${host.eureka}:9601/eureka/
  instance:
    preferIpAddress: true
keycloak:
  auth-server-url: http://${host.keycloak}:9090/auth
  enable-basic-auth: false
  realm: master
  public-client: true
  resource: fishcare
  ssl-required: none
  use-resource-role-mappings: true
  bearer-only: true
  credentials:
    secret: 418e76d1-591e-44e1-a502-9237b81105ae
  security-constraints:
    - authRoles:
        - user
      securityCollections:
        - patterns:
            - /api/dashboard/*
            - /api/user
   
